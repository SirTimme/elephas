plugins {
    id "org.example.coverage"
    id 'jacoco-report-aggregation'
}

dependencies {
    jacocoAggregation project(":modules:adder")
    jacocoAggregation project(":modules:subtracter")
    jacocoAggregation project(":modules:multiplier")
    jacocoAggregation project(":modules:divider")
}

test {
    finalizedBy testCodeCoverageReport
}

group = 'org.example'
version = '1.0-SNAPSHOT'